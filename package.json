{
  "name": "liftinglog",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "parse": "node scripts/parse-program.mjs",
    "build:catalog": "node scripts/build-catalog.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1",
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
