# Copy to .env and fill in. The anon/publishable key is public-safe (ships in the
# client bundle; Row-Level Security protects the data). Set the same two vars in
# your host's env settings for production.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
